{
  "name": "vloc-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "pnpm --filter @vloc/desktop tauri dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.0",
    "@types/node": "^25.5.0",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "turbo": "^1.12.0",
    "typescript": "^5.4.2"
  },
  "packageManager": "pnpm@8.15.5",
  "engines": {
    "node": ">=18"
  }
}
